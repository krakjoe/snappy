// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("snappy", "for snappy support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("snappy", "enable snappy support", "no");

if (PHP_SNAPPY != "no") {
	EXTENSION("snappy", "snappy.c");
}

